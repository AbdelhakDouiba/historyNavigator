#include "navigation.h"

/**
 * main - an implemntation of a simple navigator using stack
 *        data structure.
 * Return: 0 if it succeeded
 */
 
int main(void)
{
    // start the navigation
    navigate();

    return (0);
}